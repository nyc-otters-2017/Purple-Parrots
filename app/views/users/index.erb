<h2>Users</h2>

<div class="users-container">
  <ul>
    <% @users = User.all %>
      <% @users.each do |user| %>
        <li><%= user.username %></li>
        <li><%= user.answers.count %></li>
        <li><%= pretty_time(user.created_at)  %></li>
      <% end %>
  </ul>
</div>
