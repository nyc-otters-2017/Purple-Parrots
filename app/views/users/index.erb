<h2>Users</h2>

<div class="users-container">
  <ul>
    <% @users = User.all %>
      <% @users.each do |user| %>
        <li><%= user.username %></li>
        <li><%= user.answers.count %></li>
        <li><%= user.created_at.strftime("%b %d '%y at %l:%M")  %></li>
      <% end %>
  </ul>
</div>
