<h2>Users</h2>

<div class="users-container">
  <ul>
    <% @users = User.all %>
      <% @users.each do |user| %>
        <li><%= user.username %></li>
        <li><%= user.answers.count %></li>
      <% end %>
  </ul>
</div>
